name: Experimental Linux image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  explain-current-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Record image-build gate
        run: |
          echo "The live-build configuration is scaffolded but application packaging and boot tests are pending."
          echo "This workflow deliberately does not publish or release an untested image."
